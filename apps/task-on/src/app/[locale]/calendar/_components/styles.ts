import { styled } from '@mui/material';

export const CalendarWrapper = styled('div')`
  width: 100%;
  overflow-x: auto;
  margin-top: 16;

  .rbc-toolbar {
    flex-direction: column;
    gap: 12px;
    margin-bottom: 16px;
  }
`;
